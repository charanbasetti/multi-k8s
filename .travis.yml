sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t charanbasetti/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true charanbasetti/react-test npm test

after_success:
  - docker build -t charanbasetti/multi-client ./client
  - docker build -t charanbasetti/multi-nginx ./nginx
  - docker build -t charanbasetti/multi-server ./server
  - docker build -t charanbasetti/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push charanbasetti/multi-client
  - docker push charanbasetti/multi-nginx
  - docker push charanbasetti/multi-server
  - docker push charanbasetti/multi-worker